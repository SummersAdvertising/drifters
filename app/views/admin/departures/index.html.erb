Departure index <%= link_to("New", new_admin_departure_path) %> <br>
<% @departures.each do |departure| %>
  name:<%= departure.name %> = 
  type:<%= departure.filter_type %> = 
  tour:<%= departure.tour.name %> = 
  <%= departure.url %> 
  <%= link_to("Edit", edit_admin_departure_path(departure)) %>||
  <%= link_to("刪除", admin_departure_path(departure), :method => :delete, :data => {:confirm => "確定刪除？"}, class: "delete") %>
  <br>
<% end %>

<div class= "page">
  <%= paginate @departures ,:window => 2 %>
</div>